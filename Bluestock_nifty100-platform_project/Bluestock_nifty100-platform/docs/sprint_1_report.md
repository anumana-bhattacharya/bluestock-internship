# Sprint 1 - Foundation and ETL

Implemented the 12-dataset registry, universal normalization, 14-rule DQ contract,
quarantine policy, keep-last deduplication, SQLite schema/indexes, load audit,
failure log, 14 exploratory queries, and ETL tests. Verified 12,108 retained rows,
100 TTM rejects, and zero foreign-key violations.
